# 실제 값은 .env에 넣는다. .env는 git에 올리지 않는다(.gitignore 참고).
#
# 접속 문자열 구조:
#   postgresql://사용자:비밀번호@호스트:포트/데이터베이스이름?옵션
#
# 아래 값은 docker-compose.yml의 설정과 짝을 이룬다. 한쪽을 바꾸면 다른 쪽도 바꿔야 한다.
DATABASE_URL="postgresql://readlog:readlog@localhost:5432/readlog?schema=public"
